import 'package:flutter/material.dart';

class AppColors {
  // Brand colors
  static const Color primaryRed = Color(0xFFE50914);
  static const Color darkRed = Color(0xFFB81D24);
  
  // Background colors
  static const Color background = Color(0xFF000000);
  static const Color surface = Color(0xFF121212);
  static const Color cardBackground = Color(0xFF1C1C1E);
  static const Color bottomSheetBackground = Color(0xFF161616);
  
  // Text colors
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFF9E9E9E);
  static const Color textMuted = Color(0xFF666666);
  
  // Decorative colors
  static const Color ratingYellow = Color(0xFFFFB300);
  static const Color border = Color(0xFF2C2C2E);
  static const Color activeIcon = Color(0xFFE50914);
  static const Color inactiveIcon = Color(0xFF8E8E93);
  
  // Shimmer colors (for skeletons)
  static const Color shimmerBase = Color(0xFF2C2C2E);
  static const Color shimmerHighlight = Color(0xFF3A3A3C);
}
